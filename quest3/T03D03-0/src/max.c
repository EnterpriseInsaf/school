#include "stdafx.h"
#include "conio.h"
 
 
 
int _tmain(int argc, _TCHAR* argv[]){
    scanf("%d%d", &a);
    scanf("%d%d", &b);
    fflush(stdin);
    int a;
    int b;
 
    if a == int{
        if (a>b){
            printf("a");
        }
        else{
            printf("b");
        }
    }
  
    else{
        printf("n/a");
    }
        

    getch();
        return 0;
}
#include "stdio.h"
#include "math.h"
 
 
 
int main(){
    float x;
    float y;
    scanf("%d", &x);
    scanf("%d", &y);
    fflush(stdin);


    if a == float{
        z = (7e-3 * x^4 + ((22.8 * x^? - 1e3) * x + 3) / (x * x / 2) - x * (10 + x)^(2/x) - 1.01);
        printf("z");
    }
  
    else{
        printf("n/a");
    }

}